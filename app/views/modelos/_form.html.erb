<%= form_for(@modelo) do |f| %>
  <% if @modelo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@modelo.errors.count, "error") %> prohibited this modelo from being saved:</h2>

      <ul>
      <% @modelo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :modelo %><br />
    <%= f.text_field :modelo %>
  </div>
  <div class="field">
    <%= f.label :marca %><br />
    <%= select_tag :marca_id, options_from_collection_for_select(Marca.find(:all, :order => "marca ASC"), "id", "marca"), :prompt => "Selecione" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
