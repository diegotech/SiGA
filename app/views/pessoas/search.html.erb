<table>
  <tr>
    <th>Nome</th>
    <th>RG</th>
    <th>Tipo</th>
    <th>Endereço</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @pessoas.each do |pessoa| %>
  <tr>
   
    <% x = pessoa.uhs.length %>
    <td rowspan = <%= x %>>
    <%= pessoa.nome %></td>
    <td rowspan = <%= x %>>
    <%= pessoa.rg %></td>
    <td rowspan = <%= x %>>
    <%= pessoa.tipo_pessoa.tipo %></td>
    <td><% pessoa.uhs.each do |p| %>
      <%=  p.endereco  %></tr></td>
      <% end %>
    <td><%= link_to 'Editar', edit_pessoa_path(pessoa) %></td>
    <td><%= link_to 'Excluir', pessoa, confirm: 'Tem certeza que deseja excluir esta pessoa?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Pessoa', new_pessoa_path %>