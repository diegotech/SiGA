<%= form_for(@tipo_uh) do |f| %>
  <% if @tipo_uh.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_uh.errors.count, "error") %> prohibited this tipo_uh from being saved:</h2>

      <ul>
      <% @tipo_uh.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tipo %><br />
    <%= f.text_field :tipo %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
