<%= form_for(@tipo_pessoa) do |f| %>
  <% if @tipo_pessoa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_pessoa.errors.count, "error") %> prohibited this tipo_pessoa from being saved:</h2>

      <ul>
      <% @tipo_pessoa.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tipo %><br />
    <%= f.text_field :tipo %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
