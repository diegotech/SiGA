<%= form_for(@cor) do |f| %>
  <% if @cor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cor.errors.count, "error") %> prohibited this cor from being saved:</h2>

      <ul>
      <% @cor.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cor %><br />
    <%= f.text_field :cor %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
