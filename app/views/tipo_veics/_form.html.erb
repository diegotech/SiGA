<%= form_for(@tipo_veic) do |f| %>
  <% if @tipo_veic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_veic.errors.count, "error") %> prohibited this tipo_veic from being saved:</h2>

      <ul>
      <% @tipo_veic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tipo %><br />
    <%= f.text_field :tipo %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
